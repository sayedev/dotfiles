$mainMod = SUPER
$terminal = alacritty
$menu = rofi -show drun  -terse -modi "drun,calc" -no-show-match -no-sort -calc-command "echo -n '{result}' | xclip -selection clipboard" -theme $HOME/.config/rofi/config.rasi
